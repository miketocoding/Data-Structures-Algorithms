class Node {
    constructor(value) {
        this.value = value,
        this.left = null,
        this.right = null
    }
}

class BST {
    constructor() {
        // create framework for binary search tree and don't need to add new node at time tree created
        this.root = null
    }
}

let myTree = new BST()
myTree